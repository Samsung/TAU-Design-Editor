import $ from 'jquery';
import {guideInfo} from './guide-info';

describe('GuideInfo', function () {
    it('should ...', function () {
        const data = guideInfo._makeElements($('<div>'), '', {});
        expect(true).toBe(true);
    });
});
